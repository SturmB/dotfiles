{{ if .work -}}
# Atlassian API credentials
# Used by Claude Code for Jira/Confluence REST API calls
# Generate token at: https://id.atlassian.com/manage-profile/security/api-tokens
# NOTE: ATLASSIAN_API_TOKEN is age-templated from the SAME .store/jira.age as
# ~/.secrets' JIRA_API_TOKEN — rotate that one .age file to update both.

ATLASSIAN_EMAIL="cpmcgee@intxlog.com"
ATLASSIAN_API_TOKEN="{{ joinPath .chezmoi.sourceDir ".store/jira.age" | include | decrypt | trim }}"
ATLASSIAN_DOMAIN="intxlog.atlassian.net"
{{ end -}}
